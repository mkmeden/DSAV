/// <reference types="react" />
declare class nodeTrie {
    isEnd: boolean;
    children: {
        [key: string]: nodeTrie;
    };
    parent: nodeTrie;
    key: string;
    childrenJSX: JSX.Element[];
    currentJSX: JSX.Element;
    constructor(key: string);
    updateJSX(isAddHighlight?: boolean): void;
}
declare class Trie {
    root: nodeTrie;
    highlightedTill: nodeTrie;
    constructor(num?: any);
    insert(word: string): void;
    searchWord(word: string): boolean;
    deleteWord(word: string): boolean;
    findPrefix(word: string): boolean;
    findAllWords(): string[];
    findAllWordsHelper(node: nodeTrie, allWords: string[], word?: string): void;
    randomTrie(num: number): void;
    removeHighligt(): void;
}
export default Trie;
